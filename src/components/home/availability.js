import { P } from "../../data/palette";

// Shared availability metadata used by the gallery, focus view, and filters.
export const AVAILABILITY = {
  available: { key: "available", label: "Available", color: P.green },
  sold: { key: "sold", label: "Sold", color: P.magenta },
  commission: { key: "commission", label: "Commission", color: P.gold },
};
